#include <vector>
#include <string>

// not used any more

// #define vecchar std::vector<char>
// #define vecstr std::vector<std::string>

/*
vecstr makeCharVector(std::string inputStr) {
	unsigned int lenInput = inputStr.length();
	vecstr vect(lenInput);
	for (int i = 0; i < lenInput; i++)
		vect[i] = inputStr[i];
	return vect;
}
*/