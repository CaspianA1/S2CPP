#include "../Std_Lib/std_lib.hpp"
using namespace std;
auto add1 = identity([](auto x, auto y) {return add(x, 1)};);
T multiply(Q x, s y) {
return mul(x, y);
};
int main() {
identity([](auto f) {return f()};);
return 0;
}